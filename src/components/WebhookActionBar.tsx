
interface WebhookActionBarProps {
  onAddNew: () => void;
}

const WebhookActionBar = ({ onAddNew }: WebhookActionBarProps) => {
  return (
    <div className="mb-2">
      {/* Empty div to maintain spacing structure */}
    </div>
  );
};

export default WebhookActionBar;
