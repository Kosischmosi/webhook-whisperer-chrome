
import WebhookManagerContainer from "@/components/WebhookManagerContainer";

// Optimiert: Memoize the component for better performance
const WebhookManager = () => <WebhookManagerContainer />;

export default WebhookManager;
